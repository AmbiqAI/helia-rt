<div class="heliart-home not-content"><slot /></div>

<style is:global>
  .heliart-home .section-heading { display: grid; gap: var(--helia-space-4); margin-bottom: var(--helia-space-6); max-width: 58rem; }
  .heliart-home h2 { font-size: var(--helia-text-section); line-height: 1.2; letter-spacing: -0.025em; margin: 0; }
  .heliart-home p { margin: 0; line-height: 1.65; }
  .heliart-home .section-heading p { color: var(--sl-color-gray-2); }
  .heliart-home .hero-note { font-size: var(--helia-text-small); line-height: 1.6; margin-top: var(--helia-space-3); }
  .heliart-home .hero-note a { color: inherit; text-underline-offset: 0.2em; }
  .heliart-home .runtime-code { min-width: 0; }
</style>
